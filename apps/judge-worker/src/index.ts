/**
 * Phase 1 worker shell.
 *
 * Deliberately does not consume SQS or execute source code yet.
 * The execution boundary is introduced in the judge phases.
 */

console.log('DevArena judge-worker foundation ready.');
